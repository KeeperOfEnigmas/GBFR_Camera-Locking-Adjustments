v 1.0.0:
> Initial release
> Pyet-A (em3100)
    - Completely disables the locking of fireflies (we3100) and hutchison generator (we3101). 
    - Reduce the number of possible locked parts on the boss from 7 to 3 (head, chest, tail). Only affect hard lock.
> If you don't want adjustments for target parts, remove folder name emXXXX (em3100) in this path ...\gbfr.enemy.locking_fix\GBFR\data\system\enemy\data.
> This is the same for fireflies (we3100) and hutchison generator (we3101).
> 初始更新。
> 【机神】世须良加（em3100）调整。
    - 禁用流萤（we3100）与反重力蓄能器（we3101）的锁定。
    - 减少了身上可锁定的部位（从7到3，只剩头、胸、尾巴）。仅影响硬锁。
> 如果不想调整可锁定部位的话。 移除...\gbfr.enemy.locking_fix\GBFR\data\system\enemy\data这个目录里emXXXX（em3100）的文件夹就行。
> 流萤（we3100）和反重力蓄能器 （we3101）同理.

v 1.0.1:
> Remove unnecessary files.
> 移除多余的文件。

v 1.0.2: 
> Now soft lock will not target fireflies.
> Restore the locking of hutchison generator since a lot of people disagree with it. 
> Change log is now included in the zip.
> 现在软锁也不会锁定流萤了。
> 恢复了反重力蓄能器的锁定，因为不少人不喜欢。
> 压缩包现在包含更新日志。

v 1.0c:
> Remove soft lock of hands. 
> Add an option locking parts adjustments. 
    - Install different option by overwriting the main mod file. (Preserve the same paths.) 
    - Option 1: The same as before. 
    - Option 2: Ability to hard lock onto only legs and nothing else. 
> 移除手的软锁。
> 增加可锁定部位的额外选项。
    - 覆盖mod文件以安装不同选项。 （确保目录不变。）
    - Option 1: 与之前一样。
    - Option 2: 硬锁仅锁定腿。
> Version naming style change if you care.
> 修改了版本命名方式。 